# Production build for the Cloudflare Worker deployment.
# Leave EMPTY so the SPA calls same-origin /api/*, which the Worker
# (worker/index.ts) reverse-proxies to the backend via the tunnel URL in KV.
# Do NOT hardcode the tunnel URL here — quick-tunnel URLs rotate on restart.
VITE_API_BASE=
